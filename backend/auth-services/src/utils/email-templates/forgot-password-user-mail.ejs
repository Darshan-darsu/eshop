<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>Password reset</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <style>
        /* Client-specific resets */
        body,table,td,a{ -webkit-text-size-adjust:100%; -ms-text-size-adjust:100%; }
        table,td{ mso-table-lspace:0pt; mso-table-rspace:0pt; }
        img{ -ms-interpolation-mode:bicubic; }

        /* Basic styling */
        body{ margin:0; padding:0; background-color:#f4f6f8; font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif; color:#334155; }
        .email-wrapper{ width:100%; background:#f4f6f8; padding:24px 0; }
        .email-content{ width:100%; max-width:600px; margin:0 auto; background:#ffffff; border-radius:8px; overflow:hidden; box-shadow:0 2px 6px rgba(16,24,40,0.05); }
        .email-header{ padding:24px; text-align:center; background: linear-gradient(90deg,#0066ff 0%, #0052e6 100%); color:#ffffff; }
        .brand{ font-size:20px; font-weight:600; letter-spacing:0.2px; }
        .email-body{ padding:28px 28px 20px 28px; }
        h1{ margin:0 0 12px 0; font-size:20px; color:#0f172a; }
        p{ margin:0 0 16px 0; line-height:1.5; color:#475569; }
        .otp-box{ display:inline-block; padding:16px 22px; background:#f1f5f9; border-radius:8px; font-weight:700; font-size:24px; letter-spacing:4px; color:#0f172a; margin:12px 0; }
        .btn{ display:inline-block; text-decoration:none; background:#0066ff; color:#ffffff; padding:12px 18px; border-radius:8px; font-weight:600; margin-top:12px; }
        .small{ font-size:13px; color:#64748b; }
        .footer{ padding:18px 28px; font-size:13px; color:#94a3b8; text-align:center; background:#fbfdff; }
        .preheader{ display:none !important; visibility:hidden; opacity:0; color:transparent; height:0; width:0; }
        @media (max-width:480px){
            .email-body{ padding:20px; }
            .otp-box{ font-size:20px; padding:12px 16px; }
        }
    </style>
</head>
<body>
    <span class="preheader">Use the one-time code below to reset your password.</span>
    <div class="email-wrapper">
        <div class="email-content" role="article" aria-roledescription="email">
            <div class="email-header">
                <div class="brand">E-Shop</div>
            </div>

            <div class="email-body">
                <h1>Password reset</h1>
                <p>Hi <%= name ? name : 'there' %>,</p>
                <p>We received a request to reset your password. Use the one-time verification code below to continue. This code is confidential — do not share it with anyone.</p>

                <div style="text-align:center;">
                    <div class="otp-box"><%= otp %></div>
                </div>

                <p class="small">If the code above doesn't work, try copying and pasting it into the verification field. The code is valid for a limited time.</p>

                <p>If you didn't request a password reset, you can safely ignore this email. If you have any concerns, contact our support team.</p>

                <p style="margin-top:18px;">Thanks,<br/>The E-Shop Team</p>
            </div>

            <div class="footer">
                <div class="small">E-Shop • Customer Support: support@e-shop.example</div>
                <div class="small" style="margin-top:6px;">&copy; <%= new Date().getFullYear() %> E-Shop. All rights reserved.</div>
            </div>
        </div>
    </div>
</body>
</html>